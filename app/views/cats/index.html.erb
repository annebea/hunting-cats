
<h1 class="container-header title-color">The Hunters</h1>

<div class="container-index">
  <% @cats.each do |cat| %>
    <div class="card-trip" >
      <div>
        <%= cl_image_tag cat.photo.key, height: 300, width: 700, crop: :fill %>
      </div>
      <div class="card-trip-infos">
         <h2><%= link_to cat.name.upcase, cat_path(cat) %></h2>
         <p> Performance: <%= cat.performance %> </p>
         <p class="card-trip-pricing"> <%= cat.price_per_day %> € / day</p>
      </div>
    </div>
  <% end %>
</div>

<%= render 'shared/footer' %>


