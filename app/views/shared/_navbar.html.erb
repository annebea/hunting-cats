<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag "Logo-HCatsFixed-12.png" %>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to 'All Cats', cats_path, class: "btn btn-outline-primary mx-4"%>
        <% if user_signed_in? %>
          <%= link_to 'My Bookings',  bookings_path, class: "btn btn-outline-primary mx-4"%>
          <%= link_to 'Add a Cat',  new_owner_cat_path, class: "btn btn-outline-primary mx-4"%>
          <%= link_to 'All my Cat',  owner_cats_path, class: "btn btn-outline-primary mx-4"%>
          <%= link_to "My rented cats bookings", owner_bookings_path, class: "btn btn-outline-primary mx-4"%>
        <% else %>
        <% end %>
        <%= link_to 'Login', new_user_session_path, class: "btn btn-primary mx-4" unless user_signed_in? %>
      </li>
      <li class="nav-item">
        <%= link_to 'Sign-up', new_user_registration_path, class: "btn btn-primary mx-4" unless user_signed_in? %>
      </li>

      <li class="nav-item dropdown">
        <% if user_signed_in? %>
          <% unless current_user.avatar.key.nil?  %>
            <%= cl_image_tag current_user.avatar.key, class: "avatar dropdown-toggle" , id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <% end %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item " if user_signed_in? %>
          </div>
        <% end %>
      </li>
    </ul>
  </div>
</div>
